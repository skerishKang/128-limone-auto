# QUICKSTART.md - AI 분석 결과

## 📁 파일 정보
- **파일명**: QUICKSTART.md
- **카테고리**: DOCUMENT
- **분석일**: 2025-11-10 16:31:09

## 🤖 AI 분석 결과

## 실시간 통역 시스템 퀵스타트 가이드 요약

이 문서는 **5분 안에 실시간 통역 시스템을 설치하고 실행하는 방법**을 안내합니다.

**주요 단계:**

1.  **설치 (1분):** 운영체제에 맞는 설치 스크립트(`install.bat` 또는 `./install.sh`)를 실행합니다.
2.  **API 키 설정 (2분):** `.env` 파일을 생성하고 OpenAI API 키를 필수적으로 입력합니다. 선택적으로 Naver API 키도 설정할 수 있습니다.
3.  **실행 (2분):**
    *   **GUI 모드 (추천):** `run_gui.bat` 또는 `./run_gui.sh`를 실행하여 사용자 친화적인 인터페이스를 사용합니다.
    *   **CLI 모드:** `run_cli.bat` 또는 `./run_cli.sh`를 실행하여 명령줄 인터페이스를 사용합니다.
    *   **간단한 데모:** `python demo_example.py`로 데모를 실행할 수 있습니다.

**시스템 테스트:** `python test_system.py` 명령어로 시스템을 테스트합니다.

**사용법:**

*   **GUI:** 언어를 설정하고 "▶ Start Translation" 버튼을 누르면 마이크 입력이 자동으로 번역되어 텍스트와 음성으로 출력됩니다.
*   **CLI:** `--mode interactive` (ENTER 키로 녹음) 또는 `--mode realtime` (자동 연속 번역) 옵션을 사용하여 번역 모드를 선택하고 `--target-lang`으로 대상 언어를 지정합니다.

**주요 명령어:**

*   소스 언어, 대상 언어, 번역 서비스, TTS 엔진 등을 설정할 수 있습니다. (예: `python main.py --target-lang en --source-lang ko`)

**지원 언어:** 한국어, 영어, 일본어, 중국어, 스페인어, 프랑스어, 독일어를 지원합니다.

**문제 해결:**

*   **마이크 문제:** 운영체제별로 `pyaudio` 설치 가이드가 제공됩니다.
*   **Whisper 모델 오류:** `.env` 파일에서 `WHISPER_MODEL_SIZE`를 변경하여 모델 다운로드 속도를 조절할 수 있습니다.
*   **API 키 오류:** `.env` 파일 존재 여부, API 키 유효성, 크레딧 잔액을 확인합니다.
*   **성능 문제:** `.env` 파일에서 `WHISPER_DEVICE=cuda` 설정으로 GPU 사용을 활성화하거나, `WHISPER_COMPUTE_TYPE=int8`로 CPU 최적화를 할 수 있습니다.

**추가 정보:** 더 자세한 내용은 `README.md` 파일을 참고하십시오.

**시작하기:** GUI 모드를 실행하거나 해당 운영체제의 실행 스크립트를 사용하여 시스템을 시작할 수 있습니다.

---
*이 분석은 Google Gemini AI를 사용하여 자동 생성되었습니다.*
